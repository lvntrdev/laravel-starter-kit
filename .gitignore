LOCAL.md
/vendor/
/node_modules/
/stubs/node_modules/
# Wayfinder generates these per host app — never ship to consumers.
/stubs/resources/js/routes/
# CI-only symlink for @lvntr/* alias resolution; never committed.
/stubs/vendor/
# Vite build artifacts — produced by `npm run build` in CI and locally.
/stubs/public/build/
/stubs/bootstrap/ssr/
composer.lock
.phpunit.result.cache
.phpunit.cache/
.idea/
.vscode/
*.swp
*.swo
.DS_Store
package-audit-notes/
/package-promo

# AI tooling (local, not shipped to package consumers)
/plan-docs/
/PROJECT_MAP.md
/.claude/
/CLAUDE.local.md