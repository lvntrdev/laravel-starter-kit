# AI Instructions

This project keeps its canonical AI rules in:

- `AGENTS.md` for Codex-compatible agents
- `CLAUDE.md` for project architecture and team conventions
- `.claude/rules/*` for task-specific deep checklists

Follow those files instead of duplicating rules here. Prefer project-specific conventions over generic framework advice.
